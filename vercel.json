{
  "version": 2,
  "buildCommand": "npm install --force --prefix frontend && npm run build --force --prefix frontend",
  "outputDirectory": "./frontend/build",
  "installCommand": "npm install",
  "framework": null,
  "builds": [
    {
      "src": "./frontend/build",
      "use": "@vercel/static"
    },
    {
      "src": "./backend/server.js",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/backend/server.js"
    }
  ]
}
